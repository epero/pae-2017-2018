package biz.factory;

import biz.contact.ContactDto;
import biz.entreprise.EntrepriseDto;
import biz.pdc.PersonneContactDto;
import biz.stage.StageDto;
import biz.user.UserDto;

public interface BizFactory {

  /**
   * Fournit un UserDto vide.
   * 
   * @return un UserDto vide
   */
  UserDto getUserVide();

  /**
   * Fournit un PersonneContactDto vide.
   * 
   * @return un PersonneContactDto vide.
   */
  PersonneContactDto getPersonneContactVide();

  /**
   * Fournit un EntrepriseDto vide.
   * 
   * @return un EntrepriseDto vide.
   */
  EntrepriseDto getEntrepriseVide();

  /**
   * Fournit un ContactDto vide.
   * 
   * @return un ContactDto vide.
   */
  ContactDto getContactVide();

  /**
   * Fournit un StagetDto vide.
   * 
   * @return un StagetDto vide.
   */
  StageDto getStageVide();
}
