var util=(function() {

    myajax = function(config) {
        $.extend(config, {
            type: 'POST',
            url: '/',
            statusCode: {
                400: err400,
                500: err500
            }
        });
        $.ajax(config);
    }
    
    function err500(message) {
		FrontEnd.displayPage('pageFatalError');
        $('#err500').text(message.responseText);
    }
    
    function err400(message) {
        //afficherNotif(message.responseText, 'error');
    }
    
    return {
    	//err500:err500,
    	//err400:err400,
		myajax:myajax
	}
})();
